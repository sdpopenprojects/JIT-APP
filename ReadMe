# General Introduction

This repository provides the code and datasets used in this article - * *. A study of transitioning defect prediction granularity from change-level to method-level, to appear **. 

### Environment Preparation

- Python	3.10.15

```
imblearn       0. 12. 4 

numpy          1. 26. 4

optuna         3. 2. 0

pandas         1. 4. 3

sklearn        1. 5. 2

shap           0. 46. 0

optuna         4. 0. 0

matplotlab     3. 10. 1
```

### Repository Structure

- `data` : The datasets of all experiments.
  - `method`： The method level dataset.
  
  - `change`： The change level dataset.

- `demo_CLF_change.py` : The Python code for change level experiment.

- `demo_CLF_method.py` : The Python code for method level experiment.

- `get_CLF_change_shap` : The Python code for explaining change level models for each projects.

- `get_CLF_method_shap` : The Python code for explaining method level models for each projects.

- `get_change_shap_pic` : The Python code for global explanation of change level models.

- `get_method_shap_pic` : The Python code for global explanation of method level models.

- `utilities` : The code utilities used.
